using EasyCms.Web.Common;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Text;
using System.Web.Http;

namespace EasyCms.Web.API
{
    public class ShopOrderApiController : ApiController
    {
        //[HttpPost]
        //public HttpResponseMessage Post([FromBody] OrderInfoMain)
        //{
        //    string msg = string.Empty;

        //    bool isSuccess = msg == "订单提交成功";
        //    var resp = new HttpResponseMessage(HttpStatusCode.OK); 
        //    string result = JsonWithDataTable.Serialize(new { IsSuccess = isSuccess, msg = msg });
        //    resp.Content = new StringContent(result, Encoding.UTF8, "text/plain");
        //    return resp;
        //}
    }
}
